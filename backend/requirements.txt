fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
jinja2>=3.1.0
playwright>=1.40.0
httpx>=0.25.0
httpx-ws>=0.6.0
apscheduler>=3.10.0
openai>=1.0.0
asyncpg>=0.29.0
redis[hiredis]>=5.0.0
pytest>=8.0.0
psutil>=5.9.0
# File service (Подсистема II) — required by services/file_service.py + routes/files.py
python-magic>=0.4.27
Pillow>=10.0.0
python-multipart>=0.0.6
# NOTE for Windows dev: bare python-magic segfaults without libmagic. Install
# python-magic-bin additionally (pip install python-magic-bin). On Linux prod,
# the system libmagic1 (apt install libmagic1) is what python-magic binds to.
